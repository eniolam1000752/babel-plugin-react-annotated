{
  "name": "babel-plugin-react-annotated",
  "version": "1.0.2",
  "description": "annotates react statments with flow like annotations (eg. '//@state' //@useEffect)  providing for shorter code on development",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx babel ./test/src --out-dir ./test/out",
    "config-init": " cd init && node init_to_webpack.js && cd ../",
    "postinstall": "node init_to_webpack"
  },
  "publishConfig": {
    "registry": "https://github.com/eniolam1000752/babel-plugin-react-annotated"
  },
  "keywords": [
    "@state",
    "@init",
    "@consumer",
    "@provider"
  ],
  "author": "Eniola ifeoluwa olatunji",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.7.7",
    "@babel/core": "^7.7.7",
    "@babel/plugin-syntax-jsx": "^7.2.0",
    "@babel/preset-env": "^7.7.7",
    "@babel/preset-react": "^7.7.0",
    "babylon": "^6.18.0"
  }
}
